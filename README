# 项目背景
当前对模型训练过程的展示还没有成熟的工具，希望在1.0版本完成基本的计算过程可视化，后续2.0版本可以可视化多机数据，并在3.0版本提供性能优化建议

# 当前进展
1、已经完成从profiler中取出graph数据，变成json格式
2、初步完成从json到dot文件转换，并将dot转成png

# 下一步目标
1、将tensor从边变成椭圆形节点，避免有的tensor不是算子输出时无法显示
2、观察可视化结果与模型训练过程是否一致，是否可以达到可视化训练过程的效果
3、将tensor生命周期和算子生命周期显示出来
4、显示tensor的category、device、ptr（id，version）、shape、size、申请释放时间
